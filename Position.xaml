<Page x:Class="Hindalco.Position"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920" Loaded="Page_Loaded"
      Title="Position">

    <Grid>
        <!-- Smooth Gradient Background -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#E3F2FD" Offset="0.0"/>
                <!-- Light Blue -->
                <GradientStop Color="#F1F8E9" Offset="1.0"/>
                <!-- Soft Greenish -->
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Header -->
        <Border Background="#0288D1" Height="100" VerticalAlignment="Top" CornerRadius="10">
            <TextBlock Text="🏢 Select Your Position" Foreground="White" FontSize="40" 
                       VerticalAlignment="Center" HorizontalAlignment="Center" Padding="10" FontWeight="Bold"/>
        </Border>

        <!-- StackPanel for Positions -->
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Width="510">

            <Button x:Name="AdminButton" Content="👨‍💼 Admin" FontSize="36" Style="{StaticResource NumButton}" Background="#81C784" Foreground="White" 
                    Width="460" Height="90" FontWeight="Bold" BorderThickness="0" Margin="0,10,0,10" Click="Position_Click"/>

            <Button x:Name="SupervisorButton" Content="🛠️ Supervisor" FontSize="36" Style="{StaticResource NumButton}" Background="#64B5F6" Foreground="White" 
                    Width="460" Height="90" FontWeight="Bold" BorderThickness="0" Margin="0,10,0,10" Click="Position_Click"/>

            <Button x:Name="ManagerButton" Content="📊 Manager" FontSize="36" Style="{StaticResource NumButton}" Background="#FFB74D" Foreground="White" 
                    Width="460" Height="90" FontWeight="Bold" BorderThickness="0" Margin="0,10,0,10" Click="Position_Click"/>

            <Button x:Name="HRButton" Content="🤝 Human Resources (HR)" FontSize="36" Style="{StaticResource NumButton}" Background="#9575CD" Foreground="White" 
                    Width="460" Height="90" FontWeight="Bold" BorderThickness="0" Margin="0,10,0,10" Click="Position_Click"/>

            <Button x:Name="ITSupportButton" Content="💻 IT Support" FontSize="36" Style="{StaticResource NumButton}" Background="#4DB6AC" Foreground="White" 
                    Width="460" Height="90" FontWeight="Bold" BorderThickness="0" Margin="0,10,0,10" Click="Position_Click"/>

            <Button x:Name="AccountantButton" Content="📑 Accountant" FontSize="36" Style="{StaticResource NumButton}" Background="#E57373" Foreground="White" 
                    Width="460" Height="90" FontWeight="Bold" BorderThickness="0" Margin="0,10,0,10" Click="Position_Click"/>
        </StackPanel>
        <Button x:Name="Backbutton" Content="⬅ Back" Width="200" Height="50" Style="{StaticResource NumButton}" Click="Backbutton_Click" Background="Tomato" Foreground="White" FontSize="18" Margin="2,930,1718,100"/>

    </Grid>
</Page>
